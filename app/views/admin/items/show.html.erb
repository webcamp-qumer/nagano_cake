<div class="title">
  <div class="container">
    <div class="row">
      <div class="col-2">
          <h3>商品詳細</h3>
      </div>
    </div>
  </div>
</div>

<div class='container'>
  <div class='row'>
    <div class='col-3'>
      <% if @item.image.attached? %>
　    <%= image_tag @item.image, size: "200x200" %><!--image_tag=画像を表示するための記述-->
　    <% else %>
　      <%= image_tag 'no-image', size: "200x200" %>
　    <% end %>
    </div>
    
    <div class='col-7'>
    <p>商品名　<%= @item.name %></p>
    <p>商品説明　<%= @item.message %></p>
    <p>ジャンル <%= @item.genre.name %></p>
    <p>税込価格 </br>
       （税抜価格）
     <%= @item.price.to_s(:delimited) %>
    （<%= @item.price_non_tax.to_s(:delimited) %>）円</p>
    <p>販売ステータス　　
     <% if @item.is_active == true %>
     販売中
     <% else %>
     販売停止中
     <% end %>
    </p>

    <%= link_to '編集する', edit_admin_item_path(@item) %>  
</div>
